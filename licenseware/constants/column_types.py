from .base_types import BaseTypes


class ColumnTypes(BaseTypes):
    STRING = "string"
    NUMBER = "number"
    DATE = "date"
    BOOL = "bool"
    JSON = "json"
    ENUM = "enum"
    ENTITY = "entity"
